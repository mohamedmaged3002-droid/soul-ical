# Supabase project: bluekeys (zaqxfyfigprouduqtlrg)
SUPABASE_URL=https://zaqxfyfigprouduqtlrg.supabase.co
# Service-role key (write access to listing_ical). NEVER commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
# Google auth — set EITHER option (service account wins if both are present):
#
# Option B — service account (reuse the mynt-drive-adapter key). Share the Soul sheet
# with this key's client_email as Viewer. Enable the Sheets API on that GCP project.
GDRIVE_SA_JSON=
#
# Option A — OAuth as maged@bluekeys.co (no Soul share needed). Mint the refresh
# token once with: node scripts/get-refresh-token.js
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REFRESH_TOKEN=
# Soul availability spreadsheet id (from the share URL .../spreadsheets/d/<THIS>/edit)
SOUL_SHEET_ID=1du_0ygj0rs5cQRM2GMq2MNyRJJsybsMY9jjT5Rf1Djg
